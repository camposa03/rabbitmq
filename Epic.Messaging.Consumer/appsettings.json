{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "RabbitSettings": {
    "Hostname": "localhost",
    "UserName": "guest",
    "Password": "guest",
    "QueueName": "atlanta.request.person",
    "VirtualHost":  "legacy"

  }
}
